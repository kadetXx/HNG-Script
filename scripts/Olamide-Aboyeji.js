const myData = {
  name : 'Olamide Aboyeji',
  HNG_ID : 'HNG-05560',
  language : 'Javascript',
  stage : 2
};

const { name, HNG_ID, language, stage } = myData;
console.log(`Hello World, This is ${name} with HNGi7 ID ${HNG_ID} using ${language} for stage ${stage} task`);
