const myScript = (fullname, id, language) => {
  console.log(`Hello World, this is ${fullname} with HNGi7 ID ${id} using ${language} for stage 2 task`);
}
const fullname='Adeniyi Lekan Femi';
const ID ='HNG-02968';
const language='JavaScript';
myScript(fullname, ID, language);