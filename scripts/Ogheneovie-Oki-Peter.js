var name = 'Ogheneovie Oki-Peter';
var ID = 'HNG-03714';
var lang = 'JavaScript';

function showProps(myName, myID, myLang) {
    var result = ``;

        result += (`Hello World, this is ${myName} with HNGi7 ID: ${myID} using ${myLang} for stage 2 task`);
     
    return result;
  }

 
  console.log (showProps(name, ID, lang));