const myScript = (Name, id, language) => {
    console.log(`Hello World, this is ${Name} with HNGi7 ID ${id} using ${language} for stage 2 task`);
}
const Name = 'Lemovou Dachi Ivan';
const id = 'HNG-04042';
const language = 'Javascript';

myScript(Name, id, language);