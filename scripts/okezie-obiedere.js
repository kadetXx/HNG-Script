const profile = {
    name: 'Frank Okezie Obiedere',
    id: 'HNG-05247',
    email: 'obiedereokezie@gmail.com',
}

const output = `Hello World, this is ${profile.name} with HNGi7 ID ${profile.id} and email ${profile.email} using JavaScript for stage 2 task`

console.log(output);
