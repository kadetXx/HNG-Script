const user = {
  firstname: 'Timilehin',
  lastname: 'Odulate',
  id: 'HNG-00395',
  lang: 'JavaScript',
};

let message = `Hello World, this is ${user.firstname} ${user.lastname} with HNGi7 ID ${user.id} using ${user.lang} for stage 2 task.`;

console.log(message);