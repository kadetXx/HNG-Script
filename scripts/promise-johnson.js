const user = "Promise Johnson";
const id = "HNG-03039";
const lang = "Javascript";
const email = "chiemelapromise30@gmail.com";

function introMe() {
  return console.log(
    `Hello World, this is ${user} with HNGi7 ID ${id} and email ${email} using ${lang} for stage 2 task.`
  );
}

introMe();
