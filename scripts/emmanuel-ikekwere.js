// HNGi7 Internship Stage 2 task:

function Message(fullName, ID, language) {
  let output = `Hello World, this is ${fullName} with HNGi7 ID ${ID} using ${language} for stage 2 task`;
  return output;
}

console.log(Message("Emmanuel Ikekwere", "HNG-00209", "JavaScript"));
